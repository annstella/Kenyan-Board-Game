let player;
 function setup() {
    createCanvas(600,600);
    createCanvas(700,700);
     let resolution = 60;
    let resolution = 70;
    let cols = width / resolution;
    let rows = height / resolution;